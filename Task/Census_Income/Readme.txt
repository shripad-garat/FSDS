
Problem Name:- Predict Census_Income 50k> or 50k<
Aim: To build the Classification ML model to predect the Census_Income 
Objective : 
        1.Data ingestion
        2.EDA
        3.Preprocessing
        4.Pickling for the preprocessing object(save the preprocessing model)
        5.After preprocessing store data inside MONGODB
        6.Load the data from mongo db (For model bulding purpose)
        7.Model Bulding:
                  Classification: logistic regression,SVM(kernel)
        8.Hyperparameter tuning(GRID SEARCH CV)
        9.Evaluation of the model
                   Classification confusion matrix, ROC AUC score

